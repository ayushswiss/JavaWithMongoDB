This Java application is ready with basic CURD operation using MongoDB.

1. We can create the Schema (Collection)
2. We can delete the Schema (Collection)
3. We can insert document (record)
4. We can retrieve all the documents.
5. We can retrieve specific document based on filter.
6. We can delete the document.
7. We can sort the data based on specific property (column)
